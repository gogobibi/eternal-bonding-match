{
  "name": "eternal-bonding-match",
  "private": true,
  "version": "0.0.1",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev -w packages/worker\" \"npm run dev -w packages/frontend\"",
    "build": "npm run build -w packages/worker && npm run build -w packages/frontend",
    "deploy": "npm run deploy -w packages/worker && npm run deploy -w packages/frontend"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
